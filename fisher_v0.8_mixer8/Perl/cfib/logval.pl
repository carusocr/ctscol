#!c:/perl/bin/perl.exe
exit;
# use DBI;
# use lib "c:/fisher_v0.8/Perl/";
# use FshPerl;

# $side_id=shift;
# $value=shift;
# $dbfield=shift;

# my $dbh = DBI->connect($telco_mysql{dbistr},$telco_mysql{userid},$telco_mysql{passwd}) || die "Cannot connect to server\n";
# my $sth = $dbh->prepare("update lre11_io_calls set $dbfield = \'$value\' where side_id = \'$side_id\'");
# $sth->execute;
# $sth->finish;
# $dbh->disconnect;
